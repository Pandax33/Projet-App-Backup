<Window x:Class="ClientProjetDevSysGraphical.AddTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientProjetDevSysGraphical"
        mc:Ignorable="d"
        Title="{local:LangageExtension Key='Task.Add.Title'}"  MinHeight="185" MaxHeight="300" MinWidth="250" MaxWidth="600" Height="200" Width="400">
    
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="MaxWidth" Value="400" />
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Content="{local:LangageExtension Key='Task.Prompt1'}" Grid.Row="0" Grid.Column="0" />
        <TextBox x:Name="nameEntry" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <Label Content="{local:LangageExtension Key='Task.Prompt2'}" Grid.Row="1" Grid.Column="0"/>
        <TextBox x:Name="sourcePathEntry" Grid.Row="1" Grid.Column="1" Margin="5"/>
        <Button x:Name="sourcePathExplorer" Content="&#x1F4C1;" Grid.Row="1" Grid.Column="2" Margin="5" Click="sourcePathExplorer_Click"/>

        <Label Content="{local:LangageExtension Key='Task.Prompt3'}" Grid.Column="0" Grid.Row="2" />
        <TextBox x:Name="targetPathEntry" Grid.Row="2" Grid.Column="1" Margin="5"/>
        <Button x:Name="targetPathExplorer" Content="&#x1F4C1;" Grid.Row="2" Grid.Column="2" Margin="5"  Click="targetPathExplorer_Click"/>
        
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <RadioButton Content="{local:LangageExtension Key='Task.Type1'}" x:Name="typeRadioButtonComplete" GroupName="BackupType" Margin="10"/>
            <RadioButton Content="{local:LangageExtension Key='Task.Type2'}" x:Name="typeRadioButtonDifferential" GroupName="BackupType" Margin="10"/>
        </StackPanel>

        <Button Name="cancelButton"  Content="{local:LangageExtension Key='Task.Cancel'}" Grid.Row="4" Grid.Column="1" Width="50" HorizontalAlignment="Left" Click="cancelButton_Click"/>
        <Button Name="applyButton" Content="{local:LangageExtension Key='Task.Apply'}" Grid.Row="4" Grid.Column="1" Width="50" HorizontalAlignment="Right" Click="applyButton_Click"/>
    </Grid>
</Window>
