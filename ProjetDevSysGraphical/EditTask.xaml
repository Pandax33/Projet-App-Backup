<Window x:Class="ProjetDevSysGraphical.EditTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjetDevSysGraphical"
        mc:Ignorable="d"
        Title="{local:LangageExtension Key='Task.Edit.Title'}" MinHeight="175" MaxHeight="300" MinWidth="250" MaxWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{local:LangageExtension Key='Task.Prompt1'}" Grid.Column="0" Grid.Row="0"/>
        <TextBlock Name="task_name" Text="task_name" Grid.Column="1" Grid.Row="0"/>

        <TextBlock Text="{local:LangageExtension Key='Task.Prompt2'}" Grid.Column="0" Grid.Row="1" />
        <TextBox x:Name="textBoxEnterPath" Grid.Column="1" Grid.Row="1" Text="{Binding EnterPath, Mode=TwoWay}" TextChanged="textBoxEnterPath_TextChanged"/>
        <Button x:Name="EnterButton" Content="&#x1F4C1;" Grid.Row="1" Grid.Column="2" Click="ButtonEnterPath_Click" Height="20" Grid.RowSpan="2" VerticalAlignment="Top"/>

        <TextBlock Text="{local:LangageExtension Key='Task.Prompt3'}" Grid.Column="0" Grid.Row="2" />
        <TextBox x:Name="textBoxOutPath" Grid.Column="1" Grid.Row="2" Text="{Binding OutPath, Mode=TwoWay}" TextChanged="textBoxOutPath_TextChanged"/>
        <Button x:Name="DestinationButton" Content="&#x1F4C1;" Grid.Row="2" Grid.Column="2"  Click="ButtonOutPath_Click"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="5">
            <RadioButton Content="{local:LangageExtension Key='Task.Type1'}" x:Name="radioButtonComplete" GroupName="BackupType" VerticalAlignment="Center" Margin="0,0,5,0" IsChecked="{Binding IsCompleteBackup, Mode=TwoWay}" Checked="radioButtonComplete_Checked"/>
            <RadioButton Content="{local:LangageExtension Key='Task.Type2'}" x:Name="radioButtonDifferentielle" GroupName="BackupType" IsChecked="{Binding IsDifferentialBackup, Mode=TwoWay}" Checked="radioButtonDifferentielle_Checked"/>
        </StackPanel>

        <Button Name="cancelButton"  Content="{local:LangageExtension Key='Task.Cancel'}" Grid.Row="4" Grid.Column="0" Margin="5" Width="100" Click="cancelButton_Click"/>
        <Button Name="applyButton" Content="{local:LangageExtension Key='Task.Apply'}" Grid.Row="4" Grid.Column="1" Margin="5" Width="100"  Click="applyButton_Click"/>
    </Grid>
</Window>
